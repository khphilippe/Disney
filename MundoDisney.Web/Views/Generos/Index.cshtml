@model IEnumerable<MundoDisney.Web.Data.Genero>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nombre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ImageFullPath)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nombre)
            </td>
           <td>
            <img src="@item.ImageFullPath" style="width:100px;height:100px;max-width: 100%; height: auto;" />
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.GeneroId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.GeneroId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.GeneroId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
